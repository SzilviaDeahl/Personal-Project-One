extends ../layout

block content
  h1 My Saved Recipes
  p
    if user.mon
      p= user.mon.name
  p  
    if user.tues
      p= user.tues.name    
  p  
    if user.wedns
      p= user.wedns.name    
  p  
    if user.thurs
      p= user.thurs.name    
  p  
    if user.fri
      p= user.fri.name    
  p  
    if user.sat
      p= user.sat.name    
  p  
    if user.sun
      p= user.sun.name    
        
